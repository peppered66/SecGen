<?xml version="1.0"?>

<scenario xmlns="http://www.github/cliffe/SecGen/scenario"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://www.github/cliffe/SecGen/scenario">

  <name>SystemD script injection test: Kali + Debian 12 KDE</name>
  <author>William Smith</author>
  <description>
    Provisions a Kali attacker VM and a Debian 12 KDE target VM with the systemd script injection vulnerability applied.
  </description>

  <type>ctf</type>
  <difficulty>easy</difficulty>


  <!-- Target VM -->
  <system>
    <system_name>target_vm</system_name>
    <base distro="Debian 12" type="desktop" name="KDE"/>

      <!-- provision test account -->
    <utility module_path=".*/parameterised_accounts">
      <input into="accounts">
        <value>{"username":"test","password":"tiaspbiqe2r","super_user":"true"}</value>
      </input>
    </utility>
 
    <!-- Vulnerability module -->
    <vulnerability module_path=".*systemd_script_injection.*">

      <input into="strings_to_leak">
        <generator type="flag_generator"/>
      </input>

      <input into="leaked_filenames">
        <generator type="filename_generator"/>
      </input>

     <!-- Shared IP list (same pattern as wp_3x template) -->
    <input into_datastore="IP_addresses">
      <value>172.16.0.3</value> <!-- target -->
    </input>
    
      <!-- Optional, but follows the wordpress template pattern -->
      <input into="IP_address">
        <datastore access="1">IP_addresses</datastore>
      </input>
    </vulnerability>

    <network type="private_network">
      <input into="IP_address">
        <datastore access="1">IP_addresses</datastore>
      </input>
    </network>
  </system>

</scenario>
