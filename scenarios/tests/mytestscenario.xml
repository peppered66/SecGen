<?xml version="1.0"?>

<scenario xmlns="http://www.github/cliffe/SecGen/scenario"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://www.github/cliffe/SecGen/scenario">

  <name>Testing VM which provisions a kali machine and debian 12 machine installing a single vuln module for testing.</name>
  <author>William Smith</author>

  <description>
    Provisions a Debian target with Redis installed + misconfigured via the redisdb Puppet module,
    and loads two leaked strings into Redis under two chosen keys.
  </description>

  <type>ctf</type>
  <difficulty>easy</difficulty>

  <system>
    <system_name>attack_vm</system_name>
    <base distro="Kali" name="MSF"/>

    <input into_datastore="IP_addresses">
      <value>172.16.0.2</value>
      <value>172.16.0.3</value>
    </input>

    <utility module_path=".*/parameterised_accounts">
      <input into="accounts">
        <value>{"username":"kali","password":"kali","super_user":"true","strings_to_leak":[],"leaked_filenames":[]}</value>
      </input>
    </utility>

    <network type="private_network">
      <input into="IP_address">
        <datastore access="0">IP_addresses</datastore>
      </input>
    </network>

    <input into_datastore="spoiler_admin_pass">
      <generator type="strong_password_generator"/>
    </input>

    <build type="cleanup">
      <input into="root_password">
        <datastore>spoiler_admin_pass</datastore>
      </input>
    </build>
  </system>

  <system>
    <system_name>redis_target</system_name>
    <base distro="Debian 12" type="desktop" name="KDE"/>

    <network type="private_network">
      <input into="IP_address">
        <datastore access="1">IP_addresses</datastore>
      </input>
    </network>

    <vulnerability module_path=".*redisdb" privilege="user_rwx" access="remote" read_fact="strings_to_leak">
      <input into="strings_to_leak">
        <generator type="flag_generator" module_path=".*/flag_words"/>
      </input>

      <input into="leaked_filenames">
        <value>file1</value>
        <value>file2</value>
      </input>
    </vulnerability>

    <build type="cleanup">
      <input into="root_password">
        <datastore>spoiler_admin_pass</datastore>
      </input>
    </build>
  </system>

</scenario>
