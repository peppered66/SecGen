<?xml version="1.0"?>
<vulnerability xmlns="http://www.github/cliffe/SecGen/vulnerability"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="http://www.github/cliffe/SecGen/vulnerability">

  <name>Systemd script injection</name>
  <author>William Smith</author>
  <module_license>MIT</module_license>
  <description>Systemd runs a service as root which executes a vulnerable script.</description>

  <type>systemd</type>
  <privilege>user_rw</privilege>
  <access>local</access>
  <platform>linux</platform>

  <read_fact>strings_to_leak</read_fact>
  <read_fact>leaked_filenames</read_fact>
  <read_fact>strings_to_pre_leak</read_fact>
  <read_fact>pre_leak_filename</read_fact>


  <!-- flags or other secrets exposed after exploitation -->
  <default_input into="strings_to_leak">
    <generator type="message_generator"/>
  </default_input>

  <default_input into="leaked_filenames">
    <generator type="filename_generator"/>
  </default_input>


  <hint>To be implemented.</hint> 
  <solution>Discover the script, edit it to spawn a privileged shell within a directory you can access.</solution> 


</vulnerability>
