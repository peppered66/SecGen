<?xml version="1.0"?>

<vulnerability xmlns="http://www.github/cliffe/SecGen/vulnerability"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="http://www.github/cliffe/SecGen/vulnerability">
  <name>Access control mis-configurations sudo tee root</name>
  <author>Z. Cliffe. Schreuders</author>
  <module_license>Apache v2</module_license>
  <description>Misconfigure sudo to allow a low-privileged user to run tee as root without a password, enabling arbitrary file writes and privilege escalation using sudo tee.</description>

  <type>access_control_misconfiguration</type>
  <type>sudo_root_tee</type>
  <privilege>root_rwx</privilege>
  <access>local</access>
  <platform>unix</platform>

  <read_fact>username</read_fact>
  <read_fact>strings_to_leak</read_fact>
  <read_fact>leaked_filenames</read_fact>
  <read_fact>strings_to_pre_leak</read_fact>
  <read_fact>pre_leak_filename</read_fact>

  <!-- random username for the vulnerable local account -->
  <default_input into="username">
    <generator type="username_generator"/>
  </default_input>

  <!-- flags or other secrets exposed after exploitation -->
  <default_input into="strings_to_leak">
    <generator type="message_generator"/>
  </default_input>

  <default_input into="leaked_filenames">
    <generator type="filename_generator"/>
  </default_input>

  <requires>
    <module_path>utilities/unix/puppet_module/sudo</module_path>
  </requires>

  <CyBOK KA="AAA" topic="Authorisation">
    <keyword>access control</keyword>
    <keyword>elevated privileges</keyword>
    <keyword>sudo misconfiguration</keyword>
    <keyword>privilege escalation</keyword>
  </CyBOK>
  <CyBOK KA="OSV" topic="Primitives for Isolation and Mediation">
    <keyword>Access controls and operating systems</keyword>
    <keyword>Linux security model</keyword>
    <keyword>Attacks against SUDO</keyword>
    <keyword>privileged file write</keyword>
  </CyBOK>

</vulnerability>