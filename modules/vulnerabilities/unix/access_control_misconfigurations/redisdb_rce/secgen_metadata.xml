<?xml version="1.0"?>
<vulnerability xmlns="http://www.github/cliffe/SecGen/vulnerability"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="http://www.github/cliffe/SecGen/vulnerability">

  <name>Redis Replication Remote Code Execution</name>
  <author>William Smith</author>
  <module_license>MIT</module_license>
  <description>
    Redis is configured to listen on all interfaces with protected-mode disabled,
    allowing unauthenticated remote access. The service can be enumerated and
    exploited for remote code execution.
  </description>

  <type>redis</type>
  <type>remote_code_execution</type>
  <privilege>user_rwx</privilege>
  <access>remote</access>
  <platform>linux</platform>

  <read_fact>strings_to_leak</read_fact>
  <read_fact>leaked_filenames</read_fact>
  <read_fact>strings_to_store</read_fact>

  <default_input into="strings_to_leak">
    <generator type="flag_generator"/>
  </default_input>

  <default_input into="leaked_filenames">
    <generator type="filename_generator"/>
    <generator type="filename_generator"/>
  </default_input>

  <hint>Try scanning for Redis on 6379/tcp.</hint>
  <solution>Enumerate the Redis service, exploit it using Metasploit, then retrieve the flag from /var/lib/redis.</solution>

  <conflict>
    <software_name>redis_server</software_name>
  </conflict>

</vulnerability>